.env
node_modules/
*.log
.DS_Store
Thumbs.db
.vscode/
.vercel
.env*
!.env.example
coverage/

.vercel
.env*
.claude/

# Playwright
/test-results/
/playwright-report/
/blob-report/

# Demo video: the pipeline under video/ is source and is tracked; everything it
# renders is not. Frames and clips run to hundreds of megabytes, and the cut is
# reproducible from a clean checkout with `npm run video`.
/video/out/
